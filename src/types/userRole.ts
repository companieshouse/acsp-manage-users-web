export enum UserRole {
    OWNER = "owner",
    ADMIN = "admin",
    STANDARD = "standard"
}

export enum UserRoleTag {
    OWNER = "Account owner",
    ADMIN = "Administrator",
    STANDARD = "Standard user"
}
